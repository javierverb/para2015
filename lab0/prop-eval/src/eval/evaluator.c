
#include "evaluator.h"
#include "ast.h"
#include "assignment.h"
#include "misc.h"


int eval(ASTNode *f, Asgmnt *a)
{
/* Definir recursivamente 
eval(PX, a) = a find(x) 
eval (P ^ Q) = eval(P) ^ eval(Q)*/
  return 0;
}

