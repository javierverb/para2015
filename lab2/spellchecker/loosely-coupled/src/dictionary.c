#include "dictionary.h"

struct Dictionary {
    /*estructura*/
};

void dict_load(*char filename);

void dict_save(char* fname);

void dict_add(char* word);

dict_contains();
dict_new();
dict_destroy();

ignored_add();
